# ADR-0003: LinkedIn organization posting
**Status:** Accepted

We post as Company Pages (`w_organization_social`), not member profiles. Agency clients are
brands; org posting matches ownership, analytics, and admin-consent models. Member posting
is out of scope for v1 and would need a separate consent flow.
